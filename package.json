{
  "scripts": {
    "build": "rm -rf public && mkdir public && echo 'Custom ' > ./public/index.html"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "body-parser": "^1.19.1",
    "cors": "^2.8.5",
    "mongoose": "^6.1.5"
  }
}
